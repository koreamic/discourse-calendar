en:
  site_settings:
    calendar_enabled: "Allow users to create schedules?"
    calendar_schedule_default_color: "A default schedule backbroud color allowed for calendar."
  calendar:
    schedule:
      must_have_start_date_time: "Schedule must have start date time."
      validate_start_end_date_time: "The schedule's start date time must be before the end date time."
